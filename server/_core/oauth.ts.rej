diff a/server/_core/oauth.ts b/server/_core/oauth.ts	(rejected hunks)
@@ -241,7 +241,16 @@ export function registerOAuthRoutes(app: Express) {
         return;
       }
 
-      console.error("[OAuth] Google callback failed:", error);
+      const errDetail = axios.isAxiosError(error)
+        ? {
+            status: error.response?.status,
+            data: error.response?.data,
+            url: error.config?.url,
+            code: error.code,
+            message: error.message,
+          }
+        : { message: error instanceof Error ? error.message : String(error), stack: error instanceof Error ? error.stack : undefined };
+      console.error("[OAuth] Google callback failed:", JSON.stringify(errDetail));
       res.clearCookie(GOOGLE_STATE_COOKIE, cookieOptions);
       redirectWithError(res, "Falha ao entrar com Google. Tente novamente.");
     }
