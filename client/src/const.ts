export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Retorna a URL da página de login própria (sem OAuth externo)
export const getLoginUrl = () => "/login";
export const PUBLIC_LANDING_URL = "/landing-all-in-edge.html";
